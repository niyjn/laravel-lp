<?php

namespace App\Models;

/**
 * User represents the authenticatable entity in Laravel Breeze,
 * mapping directly to the Cliente model and database table.
 */
class User extends Cliente
{
    // Inherits all Cliente behaviour and database mapping.
}
